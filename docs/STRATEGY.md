# x-rider Strategy

## NOW

Create the `3neti/x-rider` package and extract the current rider concepts into package-owned contracts, DTOs, services, controllers, and UI stubs.

## NEXT

Move current rider behavior from x-change into x-rider while keeping x-change as the owner of claim execution and financial truth.

## LATER

Introduce a driver runtime inspired by form-flow, where drivers produce normalized DTOs and renderers display them.

## MUCH LATER

Introduce ads, campaigns, affiliate routing, loyalty, app-install funnels, analytics, and merchant engagement plugins.
